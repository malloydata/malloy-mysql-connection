/*import { describe, expect, test } from "@jest/globals";
import { Runtime } from "@malloydata/malloy";
// import { MySqlConnection } from "../../src/connection/mysql-connection";



describe('MySQL Connection', () {
    test('Connection works',  async () {
        const runtime = new Runtime(new MySqlConnection());
        const qm = runtime.loadQuery('SELECT 123 as blah;');
        const result = await qm.run();
        expect(JSON.stringify(result)).toBe('abc');
    });
});*/
